import React from 'react';
import { ChatAssistant } from '@/components/chat/SciChat';

const ChatPage = () => {
  return <ChatAssistant isOpen={true} onClose={() => {}} />;
};

export default ChatPage;
